# Flow Deck Mobile Client Configuration

# Default Fallback IP of the host PC
DEFAULT_HOST_IP=192.168.1.100

# Default Fallback Port of the host PC
DEFAULT_HOST_PORT=45667

# Reconnection retry interval in milliseconds
RECONNECT_INTERVAL=3000
